# ---------------------------------------------------------------
# Internal / instructor-only. Everything the repo publishes lives
# in the workshop folders (stata-onboarding/, data-wrangling/);
# anything below is prep, working notes, or generated output.
# ---------------------------------------------------------------

# Instructor prep: superset do-files, briefs, corrections, slides,
# planning docs. Never goes to students.
prep/

# Superseded root copy of the prep script. The live one is prep/prep_wrangle_data.do.
/prep_wrangle_data.do

# Session logs and verification runs (audit / handoff / roster,
# plus Stata probe scripts and their output).
log/

# Drafts and superseded material.
arc/

# --- Generated by running the workshop do-files ---
# Students produce these themselves; the folder ships without them.
# (first_looks.dta and google_trends_universities.csv are INPUTS - not ignored.)
first_looks_clean.dta
uni_trends.dta
test_log.txt
trends_scatter.png
seasons_tsline.png
combo_trends.png

# --- Generated by running the Workshop 2 do-files ---
# Students produce these themselves; the folder ships without them.
data-wrangling/state_1980.dta
data-wrangling/state_race.dta
data-wrangling/state_long.dta
data-wrangling/state_wide.dta
data-wrangling/state_long_v2.dta

# --- Stata artifacts ---
*.smcl
*.log
*.stpr
*.stswp

# --- LaTeX build artifacts ---
*.aux
*.nav
*.out
*.snm
*.toc
*.synctex.gz
*.fdb_latexmk
*.fls
*.bbl
*.blg

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
desktop.ini
*:Zone.Identifier
*~
.*.swp
.vscode/
.idea/
